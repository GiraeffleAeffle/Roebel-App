{
  "name": "@roebel/staging-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Bounded persistent NIP-01 relay for the isolated Röbel E2E namespace.",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/staging-relay.cjs",
    "start": "tsx src/cli.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netizen-labs/nostr": "workspace:*",
    "ws": "8.20.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@types/ws": "8.18.1",
    "esbuild": "^0.27.7",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
